# 2026-04-08 商品封面上传云端同步说明

这次新增了 2 个参考文件：

- `backend_code/upload.controller.js`
- `backend_code/upload.routes.js`

## 1. 备份现有路由入口

```powershell
ssh root@49.232.30.147 "cd /www/wwwroot/campus-market/backend/src && cp routes/index.js routes/index.js.bak-20260408"
```

## 2. 上传新的参考文件

```powershell
scp "C:\Users\23947\Desktop\DFL_code\backend_code\upload.controller.js" root@49.232.30.147:/www/wwwroot/campus-market/backend/src/controllers/upload.controller.js
scp "C:\Users\23947\Desktop\DFL_code\backend_code\upload.routes.js" root@49.232.30.147:/www/wwwroot/campus-market/backend/src/routes/upload.routes.js
```

## 3. 登录云服务器并更新 `src/routes/index.js`

```bash
cat > /www/wwwroot/campus-market/backend/src/routes/index.js <<'EOF'
const express = require('express');
const { healthCheck } = require('../controllers/health.controller');
const authRoutes = require('./auth.routes');
const productRoutes = require('./product.routes');
const orderRoutes = require('./order.routes');
const chatRoutes = require('./chat.routes');
const uploadRoutes = require('./upload.routes');

const router = express.Router();

router.get('/health', healthCheck);
router.use('/auth', authRoutes);
router.use('/products', productRoutes);
router.use('/orders', orderRoutes);
router.use('/conversations', chatRoutes);
router.use('/upload', uploadRoutes);

module.exports = router;
EOF
```

## 4. 做语法检查

```bash
cd /www/wwwroot/campus-market/backend/src
node -c controllers/upload.controller.js
node -c routes/upload.routes.js
node -c routes/index.js
```

## 5. 重启后端

```bash
cd /www/wwwroot/campus-market/backend
pm2 restart campus-market-backend
```

## 6. 上传接口冒烟测试

```bash
curl -X POST http://127.0.0.1:3000/api/upload/image \
  -H "Authorization: Bearer 这里替换成用户 token" \
  -F "file=@/root/test.png"
```

成功时预期返回：

```json
{
  "code": 0,
  "message": "图片上传成功",
  "data": {
    "path": "/uploads/product_xxx.png",
    "url": "http://49.232.30.147/uploads/product_xxx.png"
  }
}
```
